DEV | v1.3

### INSTALL

clone this repo
```bash

```
 
## Build the app | Docker
move to the downloaded folder and run this
```bash
cd astro & docker-compose build
```
 
later run this
 
```bash
docker-compose up -d
```